export async function downloadBackup(){const response=await fetch('/api/export');if(!response.ok)throw new Error('Không thể sao lưu dữ liệu');const blob=await response.blob();const url=URL.createObjectURL(blob);const anchor=document.createElement('a');anchor.href=url;anchor.download=`readflow-backup-${new Date().toISOString().slice(0,10)}.json`;anchor.click();URL.revokeObjectURL(url)}
export async function restoreBackup(file:File){const response=await fetch('/api/import',{method:'POST',headers:{'Content-Type':'application/json'},body:await file.text()});if(!response.ok)throw new Error('Tệp sao lưu không hợp lệ')}
