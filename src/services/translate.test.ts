import {afterEach,describe,expect,it,vi} from 'vitest';import {translateText} from './translate';
describe('translateText',()=>{afterEach(()=>vi.restoreAllMocks());it('encodes input and returns translated text',async()=>{const spy=vi.spyOn(globalThis,'fetch').mockResolvedValue(new Response(JSON.stringify({responseData:{translatedText:'xin chào'}}),{status:200}));await expect(translateText('hello world')).resolves.toBe('xin chào');expect(spy.mock.calls[0][0]).toContain('hello%20world')});it('rejects failed responses',async()=>{vi.spyOn(globalThis,'fetch').mockResolvedValue(new Response('',{status:500}));await expect(translateText('hello')).rejects.toThrow()})});
